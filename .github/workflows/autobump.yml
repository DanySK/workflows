on:
  push:
    branches:
      - master

jobs:
  Autobump:
    uses: DanySK/workflows/.github/workflows/compute-version-and-create-release.yml@0.1.0-dev80
    with:
      update-version: pre-release
