on:
  push:
    tags:
      - '*.*.*'
  workflow_dispatch:

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
    - name: Build Changelog
      uses: danysk/compute-changelog-action@0.1.30
      with:
        token: ${{ github.token }}
    - uses: ncipollo/release-action@v1
      with:
        bodyFile: "CHANGELOG.md"
        token: ${{ github.token }}
